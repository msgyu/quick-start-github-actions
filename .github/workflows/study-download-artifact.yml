name: "artifactをダウンロードする"
run-name: "artifactをダウンロードするよ"
on: [push]
jobs:
  study-artifact-download:
    runs-on: ubuntu-latest
    steps:
      - name: Download a single artifact
        uses: actions/download-artifact@v3
        with:
          name: output-log-file
